# Base URL of the GeneGenie annotation backend (Server/).
# Used server-side by the /api/annotate proxy routes (not exposed to the browser).
# Copy to .env.local to override the default.
ANNOTATION_API_BASE=http://3.6.214.176:8000
