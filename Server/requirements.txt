# FastAPI annotation service.
# Lower-bound pins so pip resolves to current wheels (incl. Python 3.14 builds).
fastapi>=0.115
uvicorn[standard]>=0.34
python-multipart>=0.0.20
pydantic>=2.11
pydantic-settings>=2.7

# Genomics: VCF region queries are done via the system `tabix` CLI (htslib),
# installed by scripts/01_install_tools.sh — no compiled Python bindings needed.

# Storage
boto3>=1.35
SQLAlchemy>=2.0.36
