# Copy to .env and adjust as needed.
CLINVAR_VCF=data/clinvar.vcf.gz
S3_BUCKET=indian-genomics-data
AWS_REGION=ap-south-1
DATABASE_URL=sqlite:///data/annotations.db
# Later (Step 6+): DATABASE_URL=postgresql+psycopg://user:pass@host:5432/genegenie
# Later (Step 5):  DBSNP_VCF=data/dbsnp_chr22.vcf.gz
